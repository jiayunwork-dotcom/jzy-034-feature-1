services:
  richards:
    build: .
    image: richards-service:latest
    ports:
      - "8080:8080"
    environment:
      LISTEN_ADDR: ":8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/healthz"]
      interval: 15s
      timeout: 3s
      retries: 3
